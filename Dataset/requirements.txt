neo4j>=5.0.0
click>=8.0.0
tabulate>=0.9.0
requests>=2.25.0
pandas>=1.3.0
python-dotenv>=0.19.0

# Optional GUI frameworks (choose one):
# PyQt5>=5.15.0        # Advanced GUI (requires installation)
# tkinter              # Built-in Python GUI (no installation needed)
